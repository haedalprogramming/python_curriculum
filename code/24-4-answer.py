scores = [88, 50, 100, 72, 95, 20, 100]
print(f"전체 점수 목록: {scores}")
print("-" * 30)

# 1. 버블 정렬로 내림차순 정렬하기
# 목표: 100, 100, 95, 88 ... 순서로 만들기

n = len(scores)

for i in range(n):
    for j in range(0, n - i - 1):
        
        # [핵심 변경 포인트]
        # 내림차순(큰 게 앞으로)이므로
        # 앞의 점수(j)가 뒤의 점수(j+1)보다 '작으면' 뒤로 보냅니다.
        if scores[j] < scores[j + 1]:  
            # 자리 바꾸기 (Swap)
            scores[j], scores[j + 1] = scores[j + 1], scores[j]

print(f"정렬 후(랭킹순): {scores}")
print("-" * 30)

print("🏆 명예의 전당 (TOP 3) 🏆")

# 2. 상위 3명 출력
# 정렬이 끝났으므로 앞의 3개가 가장 큰 점수입니다.
print(f"🥇 1등: {scores[0]}점")
print(f"🥈 2등: {scores[1]}점")
print(f"🥉 3등: {scores[2]}점")

# (심화) for문 사용 시
# for i in range(3):
#     print(f"{i+1}등: {scores[i]}점")