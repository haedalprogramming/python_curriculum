# ==========================================
# [정답 코드] 도전 과제: 웹 브라우저 뒤로 가기
# ==========================================

# 방문 기록을 저장할 스택
history = []
current_site = "Google" # 시작 페이지

while True:
    print(f"\n🌐 현재 페이지: [{current_site}]")
    print(f"   (뒤로 갈 수 있는 페이지 수: {len(history)}개)")
    
    choice = input("1.사이트 이동  2.뒤로 가기  q.종료 : ")
    
    if choice == "1":
        # 이동하기 전에, 현재 사이트를 역사(스택)에 저장!
        history.append(current_site)
        
        # 새로운 사이트로 이동
        new_site = input("이동할 사이트 이름: ")
        current_site = new_site
        
    elif choice == "2":
        # 뒤로 가기 (Pop)
        if len(history) == 0:
            print("❌ 더 이상 뒤로 갈 수 없습니다! (첫 페이지)")
        else:
            # 스택에서 가장 최근 사이트를 꺼내서 현재 사이트로 설정
            prev_site = history.pop()
            print(f"🔙 '{current_site}'에서 '{prev_site}'로 돌아갑니다.")
            current_site = prev_site
            
    elif choice == "q":
        print("인터넷을 종료합니다.")
        break
