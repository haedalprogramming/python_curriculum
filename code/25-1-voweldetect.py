# 문제: 영어 단어를 입력받아서, 그 안에 모음(a, e, i, o, u)이 총 몇 개 있는지 세어보세요.
# 힌트: 모음들을 모아둔 리스트를 따로 만들어서 하면 쉽습니다.

print("--- 🔡 영어 단어 모음 세기 ---")
word = input("영어 단어를 입력하세요: ")

# 1. 모음 리스트 정의 (이 글자들을 찾을 겁니다)
vowels = ["a", "e", "i", "o", "u"]
count = 0 # 개수를 셀 변수

# 2. 글자 하나하나 확인하기
# 문자열도 리스트처럼 for문을 돌릴 수 있습니다.
for char in word:
    # 만약 지금 글자(char)가 모음 리스트 안에 들어있다면?
    if char in vowels:
        count += 1 # 개수 1 증가
        # (선택) 어떤 글자인지 확인해보고 싶다면:
        # print(f"모음 발견! -> {char}")

print("-" * 30)
print(f"입력한 단어: {word}")
print(f"모음의 개수: {count}개")
