# [슬라이드 기획안] 12차시: time/datetime 모듈

## 1. 기획 개요

| 항목 | 내용 |
|------|------|
| **수업명** | [해달에듀] 파이썬 프로그래밍 12차시 - 시간 다루기 |
| **대상** | 초중고 학생 (코딩 초보자) |
| **총 슬라이드** | 35장 |
| **학습 목표** | time과 datetime 모듈을 활용하여 시간 관련 프로그램을 만들 수 있다 |
| **실습 코드** | `12-1-time_concept.py`, `12-2-practice_countdown.py`, `12-3-practice_timer.py` |
| **디자인 컨셉** | 메인 컬러: #FFD506, 시계/타이머 비유 활용 |

---

## 2. 상세 슬라이드 구성안

### PART 1. 도입 (Intro) : 4장

**목표: 흥미 유발 및 시간 모듈 소개**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **01** | 표지 | **[제목] 시간을 다뤄보자!** ⏰ time & datetime 모듈 **[부제] 12차시 \| 해달에듀** | 배경: #FFD506, 시계 이모지 |
| **02** | 질문 | **프로그램에서 시간이 필요할 때?** ⏱️ 타이머, 스톱워치 📅 D-day 계산기 🕐 현재 시각 표시 **파이썬으로 다 할 수 있어요!** | 시간 관련 아이콘 |
| **03** | 미리보기 | **오늘의 완성작!** ⏳ 카운트다운 타이머 🎯 10초 맞추기 게임 | 2개 완성작 미리보기 |
| **04** | 목표 | **오늘의 미션!** ☐ 1. time 모듈 사용법 ☐ 2. datetime으로 날짜 다루기 ☐ 3. 카운트다운 타이머 만들기 ☐ 4. 10초 맞추기 게임 만들기 | 점선 박스, 체크박스 |

---

### PART 2. time 모듈 (Concept 1) : 6장

**목표: time 모듈 기본 사용법**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **05** | 개념 | **time 모듈이란?** ⏰ 시간 관련 기능을 모아놓은 도구 상자! ```python import time ``` 먼저 import 해야 사용 가능! | 도구 상자 비유 |
| **06** | 코드 | **time.sleep() - 잠시 멈추기** ```python import time print("시작!") time.sleep(3)  # 3초 대기 print("3초 지났어요!") ``` 프로그램을 잠시 멈춤 | 코드 + 결과 |
| **07** | 코드 | **time.time() - 현재 시간** ```python import time now = time.time() print(now)  # 1700000000.123456 ``` 1970년 1월 1일부터 지금까지 초! (타임스탬프) | 코드 + 설명 |
| **08** | 코드 | **시간 측정하기** ```python import time start = time.time() # 여기에 측정할 코드 for i in range(1000000):     pass end = time.time() print(f"걸린 시간: {end - start:.3f}초") ``` | 코드 + 측정 개념 |
| **09** | 코드 | **time.ctime() - 읽기 쉬운 시간** ```python import time print(time.ctime()) # Sat Nov 18 15:30:45 2024 ``` 사람이 읽기 쉬운 형태로! | 코드 + 결과 |
| **10** | 정리 | **time 모듈 정리** | 함수 | 기능 | |------|------| | `time.sleep(초)` | 대기 | | `time.time()` | 타임스탬프 | | `time.ctime()` | 현재 시각 문자열 | | time 함수 표 |

---

### PART 3. datetime 모듈 (Concept 2) : 6장

**목표: datetime으로 날짜/시간 다루기**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **11** | 개념 | **datetime 모듈** 📅 날짜와 시간을 더 세밀하게! ```python from datetime import datetime, date, timedelta ``` | datetime 소개 |
| **12** | 코드 | **현재 날짜/시간** ```python from datetime import datetime now = datetime.now() print(now)  # 2024-11-18 15:30:45.123456 print(now.year)   # 2024 print(now.month)  # 11 print(now.day)    # 18 print(now.hour)   # 15 print(now.minute) # 30 ``` | 코드 + 속성들 |
| **13** | 코드 | **날짜 포맷팅** ```python from datetime import datetime now = datetime.now() # strftime = string format time print(now.strftime("%Y년 %m월 %d일")) # 2024년 11월 18일 print(now.strftime("%H:%M:%S")) # 15:30:45 print(now.strftime("%A"))  # Monday ``` | 코드 + 포맷 코드 |
| **14** | 개념 | **주요 포맷 코드** | 코드 | 의미 | 예시 | |------|------|------| | %Y | 연도 (4자리) | 2024 | | %m | 월 | 11 | | %d | 일 | 18 | | %H | 시 (24시간) | 15 | | %M | 분 | 30 | | %S | 초 | 45 | | 포맷 코드 표 |
| **15** | 코드 | **날짜 계산 (timedelta)** ```python from datetime import datetime, timedelta now = datetime.now() # 7일 후 after_week = now + timedelta(days=7) print(after_week) # 100일 후 (D+100) d_day = now + timedelta(days=100) print(d_day.strftime("%Y-%m-%d")) ``` | 코드 + 날짜 계산 |
| **16** | 코드 | **D-day 계산** ```python from datetime import datetime target = datetime(2024, 12, 25)  # 크리스마스 now = datetime.now() diff = target - now print(f"크리스마스까지 {diff.days}일!") ``` 두 날짜의 차이 계산! | 코드 + 결과 |

---

### PART 4. 실습 A - 카운트다운 (Practice A) : 6장

**목표: 카운트다운 타이머 만들기**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **17** | 안내 | **실습 A: 카운트다운 타이머** ⏳ 분:초 형태로 카운트다운! 00:00이 되면 알림! | 타이머 이미지 |
| **18** | 실습 | **1단계: 시간 입력** ```python total_seconds = int(input("몇 초? ")) ``` | 코드 블록 |
| **19** | 실습 | **2단계: 분:초 계산** ```python import time while total_seconds > 0:     mins = total_seconds // 60     secs = total_seconds % 60     print(f"\r{mins:02d}:{secs:02d}", end="")     time.sleep(1)     total_seconds -= 1 ``` `\r` = 같은 줄에서 덮어쓰기! | 코드 + \r 설명 |
| **20** | 실습 | **3단계: 완성 코드** ```python import time def countdown(total_seconds):     while total_seconds > 0:         mins = total_seconds // 60         secs = total_seconds % 60         print(f"\r⏳ {mins:02d}:{secs:02d}", end="")         time.sleep(1)         total_seconds -= 1     print("\r🔔 시간 끝!      ") # 입력 받기 seconds = int(input("몇 초 타이머? ")) countdown(seconds) ``` | 전체 코드 |
| **21** | 완료 | **실행 결과** ``` 몇 초 타이머? 10 ⏳ 00:10 → 00:09 → ... → 00:01 🔔 시간 끝! ``` | 터미널 결과 |
| **22** | 확장 | **확장: 분 단위 입력** ```python mins = int(input("몇 분? ")) secs = int(input("몇 초? ")) total = mins * 60 + secs countdown(total) ``` | 확장 코드 |

---

### PART 5. 실습 B - 10초 맞추기 (Practice B) : 6장

**목표: 10초 맞추기 게임**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **23** | 안내 | **실습 B: 10초 맞추기 게임** 🎯 시계를 안 보고 정확히 10초를 맞춰보세요! 얼마나 정확하게 맞출 수 있을까? | 게임 안내 |
| **24** | 실습 | **게임 로직** ```python import time print("🎮 10초 맞추기 게임!") print("10초라고 생각되면 엔터를 누르세요!") input("준비됐으면 엔터... ") start = time.time() input()  # 엔터 대기 end = time.time() elapsed = end - start ``` | 코드 블록 |
| **25** | 실습 | **결과 판정** ```python diff = abs(elapsed - 10) print(f"\n⏱️ 측정 시간: {elapsed:.2f}초") print(f"📏 오차: {diff:.2f}초") if diff < 0.5:     print("🏆 대단해요! 거의 완벽!") elif diff < 1:     print("👍 아주 좋아요!") elif diff < 2:     print("😊 괜찮아요!") else:     print("💪 다시 도전해보세요!") ``` | 코드 + 판정 |
| **26** | 실습 | **완성 코드** ```python import time def play_game():     print("\\n🎮 10초 맞추기 게임!")     input("준비됐으면 엔터...")     print("시작! 10초 뒤에 엔터를 누르세요!")     start = time.time()     input()     end = time.time()     elapsed = end - start     diff = abs(elapsed - 10)     print(f"\\n⏱️ 결과: {elapsed:.2f}초")     print(f"오차: {diff:.2f}초")     return diff # 게임 실행 play_game() ``` | 전체 코드 |
| **27** | 완료 | **실행 결과** ``` 🎮 10초 맞추기 게임! 준비됐으면 엔터... 시작! 10초 뒤에 엔터를 누르세요! ⏱️ 결과: 10.23초 오차: 0.23초 🏆 대단해요! 거의 완벽! ``` | 터미널 결과 |
| **28** | 확장 | **확장: 최고 기록 저장** ```python best = float('inf') while True:     diff = play_game()     if diff < best:         best = diff         print(f"🎉 최고 기록 갱신! {best:.2f}초")     again = input("다시? (y/n): ")     if again != 'y':         break print(f"최종 기록: {best:.2f}초") ``` | 확장 코드 |

---

### PART 6. 추가 활용 (Advanced) : 3장

**목표: 실용적인 예제**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **29** | 코드 | **디지털 시계** ```python import time from datetime import datetime while True:     now = datetime.now()     clock = now.strftime("%H:%M:%S")     print(f"\r🕐 {clock}", end="")     time.sleep(1) ``` Ctrl+C로 종료 | 코드 블록 |
| **30** | 코드 | **코드 실행 시간 측정** ```python import time def measure_time(func):     start = time.time()     func()     end = time.time()     print(f"실행 시간: {end-start:.4f}초") def slow_function():     total = 0     for i in range(10000000):         total += i measure_time(slow_function) ``` | 코드 블록 |
| **31** | 코드 | **나이 계산기** ```python from datetime import datetime birth_year = int(input("태어난 연도: ")) birth_month = int(input("태어난 월: ")) birth_day = int(input("태어난 일: ")) birth = datetime(birth_year, birth_month, birth_day) now = datetime.now() age = now.year - birth.year if (now.month, now.day) < (birth.month, birth.day):     age -= 1 print(f"만 {age}세입니다!") ``` | 코드 블록 |

---

### PART 7. 마무리 (Finish) : 4장

**목표: 정리 및 다음 차시 예고**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **32** | 도전 | **도전 과제!** 🏆 스톱워치 만들기! 엔터를 누르면 시작 다시 엔터를 누르면 정지 경과 시간을 표시! | 도전 문제 |
| **33** | 정리 | **오늘 배운 것** ✅ time.sleep(): 대기 ✅ time.time(): 시간 측정 ✅ datetime.now(): 현재 날짜/시간 ✅ strftime(): 날짜 포맷팅 ✅ timedelta: 날짜 계산 | 체크리스트 |
| **34** | 예고 | **다음 시간에는...** 🐢 turtle로 그림을 그려요! 거북이를 움직여서 도형 그리기! | 배경: #FFD506, 거북이 이모지 |
| **35** | 엔딩 | **수고했어요!** ⏰ 이제 시간을 자유롭게 다룰 수 있어요! **12차시 완료** | 엔딩 로고 |

---

## 3. 제작 시 유의사항 (Checklist)

1. **import 강조**: time, datetime 각각 import 방법 설명
2. **타임스탬프 개념**: 1970년부터의 초 개념 간단히
3. **\\r 설명**: 같은 줄에서 덮어쓰기 효과
4. **포맷 코드**: 자주 쓰는 것 위주로 정리
5. **실습 코드**: `code/12-*.py` 파일과 일치 확인

---

## 4. 관련 코드 파일

| 파일명 | 내용 |
|--------|------|
| `12-1-time_concept.py` | time 모듈 개념 |
| `12-2-practice_countdown.py` | 실습 A: 카운트다운 |
| `12-3-practice_timer.py` | 실습 B: 10초 맞추기 |
| `12-4-challenge.py` | 도전 과제 (스톱워치) |
| `12-5-answer.py` | 도전 과제 정답 |
