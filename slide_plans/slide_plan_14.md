# [슬라이드 기획안] 14차시: turtle 그래픽 응용

## 1. 기획 개요

| 항목 | 내용 |
|------|------|
| **수업명** | [해달에듀] 파이썬 프로그래밍 14차시 - turtle 그래픽 응용 |
| **대상** | 초중고 학생 (코딩 초보자) |
| **총 슬라이드** | 35장 |
| **학습 목표** | turtle 모듈의 고급 기능을 활용하여 애니메이션과 인터랙티브 그래픽을 만들 수 있다 |
| **실습 코드** | `14-1-turtle_advanced.py`, `14-2-practice_spiral.py`, `14-3-practice_mouse.py` |
| **디자인 컨셉** | 메인 컬러: #FFD506, 애니메이션/인터랙션 강조 |

---

## 2. 상세 슬라이드 구성안

### PART 1. 도입 (Intro) : 4장

**목표: 흥미 유발 및 turtle 응용 소개**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **01** | 표지 | **[제목] 거북이 고급 기술!** 🐢✨ turtle 그래픽 응용 **[부제] 14차시 \| 해달에듀** | 배경: #FFD506, 거북이 이모지 |
| **02** | 질문 | **더 멋진 그림을 그리려면?** 🌀 소용돌이, 프랙탈 🖱️ 마우스로 그림 그리기 ⌨️ 키보드로 거북이 조종! | 고급 기능 예시 |
| **03** | 미리보기 | **오늘의 완성작!** 🌀 무지개 소용돌이 🖱️ 마우스 추적 그리기 | 2개 완성작 미리보기 |
| **04** | 목표 | **오늘의 미션!** ☐ 1. 소용돌이/나선 그리기 ☐ 2. 이벤트 처리 (마우스, 키보드) ☐ 3. 애니메이션 만들기 ☐ 4. 나만의 작품 완성! | 점선 박스, 체크박스 |

---

### PART 2. 소용돌이와 나선 (Concept 1) : 6장

**목표: 반복과 변화를 활용한 패턴**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **05** | 개념 | **소용돌이의 원리** 🌀 반복하면서 조금씩 변화! - 거리가 점점 길어지거나 - 각도가 조금씩 달라지면 소용돌이 완성! | 소용돌이 원리 |
| **06** | 코드 | **사각형 소용돌이** ```python import turtle t = turtle.Turtle() t.speed(0) for i in range(100):     t.fd(i * 2)   # 거리 증가     t.rt(90)      # 90도 회전 turtle.done() ``` | 코드 + 결과 |
| **07** | 코드 | **삼각형 소용돌이** ```python for i in range(100):     t.fd(i * 2)     t.rt(120)  # 삼각형 각도 ``` 각도를 바꾸면 모양이 바뀌어요! | 코드 + 결과 |
| **08** | 코드 | **원형 나선** ```python t.speed(0) for i in range(200):     t.circle(i / 2, 10)  # 반지름 증가 ``` circle(반지름, 각도)로 부분 원! | 코드 + 나선 |
| **09** | 코드 | **무지개 소용돌이** ```python import turtle colors = ["red", "orange", "yellow", "green", "blue", "purple"] t = turtle.Turtle() t.speed(0) for i in range(180):     t.pencolor(colors[i % 6])     t.fd(i)     t.rt(59)  # 소수를 쓰면 더 복잡한 패턴 turtle.done() ``` | 코드 + 무지개 소용돌이 |
| **10** | 실습 | **각도 실험** ```python # 다양한 각도로 실험해보세요! for angle in [91, 89, 60, 72, 144]:     for i in range(50):         t.fd(i * 2)         t.rt(angle)     t.home() ``` 1도만 바뀌어도 완전히 다른 그림! | 실험 제안 |

---

### PART 3. 마우스 이벤트 (Concept 2) : 6장

**목표: 마우스 클릭으로 그리기**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **11** | 개념 | **이벤트(Event)란?** 🖱️ 마우스 클릭, 키보드 입력 등 사용자의 행동을 감지! "클릭하면 이것을 해라" | 이벤트 개념 |
| **12** | 코드 | **클릭 위치로 이동** ```python import turtle screen = turtle.Screen() t = turtle.Turtle() def go_to_click(x, y):     t.goto(x, y) screen.onclick(go_to_click) screen.mainloop() ``` `onclick` = 클릭할 때 함수 실행 | 코드 + 설명 |
| **13** | 코드 | **클릭하면 도장 찍기** ```python import turtle screen = turtle.Screen() t = turtle.Turtle() t.shape("turtle") t.penup() def stamp_here(x, y):     t.goto(x, y)     t.stamp()  # 도장 찍기 screen.onclick(stamp_here) screen.mainloop() ``` | 코드 + 도장 |
| **14** | 코드 | **클릭하면 원 그리기** ```python import random def draw_circle(x, y):     t.penup()     t.goto(x, y)     t.pendown()     color = random.choice(["red", "blue", "green", "yellow"])     t.pencolor(color)     t.fillcolor(color)     t.begin_fill()     t.circle(20)     t.end_fill() screen.onclick(draw_circle) ``` | 코드 + 랜덤 원 |
| **15** | 코드 | **드래그로 그리기** ```python def drag(x, y):     t.ondrag(None)  # 재귀 방지     t.goto(x, y)     t.ondrag(drag) t.ondrag(drag) screen.mainloop() ``` 마우스를 누른 채로 이동하면 그림! | 코드 + 드래그 |
| **16** | 실습 | **마우스 그림판** ```python import turtle screen = turtle.Screen() t = turtle.Turtle() t.pensize(3) t.speed(0) def start_draw(x, y):     t.pendown()     t.goto(x, y) def stop_draw(x, y):     t.penup() t.ondrag(start_draw) screen.onclick(stop_draw, 3)  # 우클릭 screen.mainloop() ``` | 간단 그림판 |

---

### PART 4. 키보드 이벤트 (Concept 3) : 5장

**목표: 키보드로 거북이 조종**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **17** | 개념 | **키보드 이벤트** ⌨️ 특정 키를 누르면 함수 실행! `screen.onkey(함수, "키")` | 키보드 이벤트 |
| **18** | 코드 | **방향키로 이동** ```python import turtle screen = turtle.Screen() t = turtle.Turtle() t.shape("turtle") def go_up():     t.setheading(90)     t.forward(20) def go_down():     t.setheading(270)     t.forward(20) def go_left():     t.setheading(180)     t.forward(20) def go_right():     t.setheading(0)     t.forward(20) screen.onkey(go_up, "Up") screen.onkey(go_down, "Down") screen.onkey(go_left, "Left") screen.onkey(go_right, "Right") screen.listen()  # 키 입력 받기 screen.mainloop() ``` | 코드 블록 |
| **19** | 코드 | **펜 올리기/내리기** ```python def pen_up():     t.penup() def pen_down():     t.pendown() screen.onkey(pen_up, "u") screen.onkey(pen_down, "d") ``` u키: 펜 올리기, d키: 펜 내리기 | 코드 + 기능 |
| **20** | 코드 | **색상 바꾸기** ```python colors = ["red", "orange", "yellow", "green", "blue"] color_index = 0 def change_color():     global color_index     color_index = (color_index + 1) % len(colors)     t.pencolor(colors[color_index]) screen.onkey(change_color, "c") ``` c키: 색상 변경 | 코드 + 색상 변경 |
| **21** | 코드 | **초기화/종료** ```python def clear_screen():     t.clear()     t.home() def quit_program():     screen.bye() screen.onkey(clear_screen, "r")  # reset screen.onkey(quit_program, "q")   # quit ``` | 코드 + 기능 |

---

### PART 5. 실습 A - 소용돌이 (Practice A) : 5장

**목표: 무지개 소용돌이 완성**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **22** | 안내 | **실습 A: 무지개 소용돌이** 🌀🌈 컬러풀한 소용돌이를 그려요! | 소용돌이 이미지 |
| **23** | 실습 | **배경 설정** ```python import turtle screen = turtle.Screen() screen.bgcolor("black")  # 검은 배경 t = turtle.Turtle() t.speed(0) t.pensize(2) ``` | 코드 블록 |
| **24** | 실습 | **무지개 색상** ```python colors = ["red", "orange", "yellow", "green", "cyan", "blue", "purple"] for i in range(200):     t.pencolor(colors[i % len(colors)])     t.fd(i)     t.rt(59) ``` 59도 = 멋진 패턴! | 코드 블록 |
| **25** | 완료 | **완성 결과** [검은 배경에 무지개 소용돌이] 🎨 멋진 작품 완성! | 결과 이미지 |
| **26** | 확장 | **확장 아이디어** ```python # 원형 소용돌이 for i in range(200):     t.pencolor(colors[i % 7])     t.circle(i / 2, 20) # 나비 패턴 for i in range(200):     t.pencolor(colors[i % 7])     t.circle(i, 60) ``` | 확장 코드 |

---

### PART 6. 실습 B - 마우스 추적 (Practice B) : 5장

**목표: 마우스 그림판 만들기**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **27** | 안내 | **실습 B: 마우스 그림판** 🖱️ 마우스로 자유롭게 그림 그리기! 색상도 바꿔가면서! | 그림판 이미지 |
| **28** | 실습 | **기본 설정** ```python import turtle screen = turtle.Screen() screen.title("🎨 마우스 그림판") t = turtle.Turtle() t.speed(0) t.pensize(3) t.penup() colors = ["red", "orange", "yellow", "green", "blue", "purple", "black"] current_color = 0 ``` | 코드 블록 |
| **29** | 실습 | **이벤트 함수** ```python def draw(x, y):     t.goto(x, y) def start(x, y):     t.pendown()     draw(x, y) def change_color():     global current_color     current_color = (current_color + 1) % len(colors)     t.pencolor(colors[current_color]) def clear():     t.clear()     t.penup()     t.home() ``` | 코드 블록 |
| **30** | 실습 | **이벤트 연결** ```python t.ondrag(draw) screen.onclick(start) screen.onkey(change_color, "c") screen.onkey(clear, "r") screen.listen() print("드래그: 그리기, c: 색상변경, r: 지우기") screen.mainloop() ``` | 코드 블록 |
| **31** | 완료 | **실행 결과** 마우스로 자유롭게 그림! [그림판 사용 예시] | 결과 이미지 |

---

### PART 7. 마무리 (Finish) : 4장

**목표: 정리 및 다음 차시 예고**

| 번호 | 유형 | 화면 구성 및 내용 | 디자인/연출 포인트 |
|------|------|-------------------|-------------------|
| **32** | 도전 | **도전 과제!** 🏆 거북이 게임 만들기! 방향키로 거북이를 조종하고, 화면에 아이템을 먹으면 점수 증가! | 게임 아이디어 |
| **33** | 정리 | **오늘 배운 것** ✅ 소용돌이/나선 패턴 ✅ screen.onclick(): 마우스 클릭 ✅ screen.onkey(): 키보드 입력 ✅ screen.listen(): 입력 대기 ✅ t.ondrag(): 드래그 | 체크리스트 |
| **34** | 예고 | **다음 시간에는...** 🌐 webbrowser 모듈! 파이썬으로 웹사이트 열기! | 배경: #FFD506, 웹 이모지 |
| **35** | 엔딩 | **수고했어요!** 🐢✨ 인터랙티브한 그래픽 프로그램 완성! **14차시 완료** | 엔딩 로고 |

---

## 3. 제작 시 유의사항 (Checklist)

1. **이벤트 개념**: 클릭/키입력 → 함수 실행 연결
2. **screen.listen()**: 키보드 입력 시 필수!
3. **mainloop()**: 이벤트 대기 루프
4. **global 변수**: 함수 안에서 전역변수 변경 시 필요
5. **실습 코드**: `code/14-*.py` 파일과 일치 확인

---

## 4. 관련 코드 파일

| 파일명 | 내용 |
|--------|------|
| `14-1-turtle_advanced.py` | turtle 고급 개념 |
| `14-2-practice_spiral.py` | 실습 A: 소용돌이 |
| `14-3-practice_mouse.py` | 실습 B: 마우스 그림판 |
| `14-4-challenge.py` | 도전 과제 (거북이 게임) |
| `14-5-answer.py` | 도전 과제 정답 |
